packages:
  - "packages/*"
  - "apps/*"
  - "examples/*"

# Pre-approve the only dependency with an install script (esbuild, pulled in by
# tsx and tsup) so pnpm doesn't block it or prompt — important for CI/Docker.
allowBuilds:
  esbuild: true
